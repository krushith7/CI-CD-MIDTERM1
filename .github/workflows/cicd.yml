name: Java CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Compile the application
      run: javac Main.java

    - name: Run the application
      run: java Main

    - name: Test the multiply method
      run: |
        echo "Testing the multiply method"
        javac Main.java
        expected=9
        result=$(java Main 3 3)
        if [ "$result" -ne "$expected" ]; then
          echo "Test failed: expected $expected but got $result"
          exit 1
        else
          echo "Test passed: $result is correct"
        fi